'use client'

import { useEffect, useMemo, useRef, useState } from 'react'
import { getAccessToken, getUser, signIn, signOut } from '@/lib/auth'

type View='overview'|'districts'|'licensing'|'users'|'themes'|'branding'|'workspaces'|'releases'|'diagnostics'|'activity'|'settings'
type DistrictForm={organization:any;domains:any[];admins:any[];allowedUsers:any[]}
type ThemeDraft={id?:string;name:string;slug:string;organizationId:string;definition:any}
type RowKind='domains'|'admins'|'allowedUsers'

const NAV:[View,string][]=[['overview','Overview'],['districts','Districts'],['licensing','Licensing & Billing'],['users','Users & Seats'],['themes','Theme Studio'],['branding','Branding'],['workspaces','Workspaces'],['releases','Releases'],['diagnostics','Diagnostics'],['activity','Activity'],['settings','Owner Settings']]
const baseDef={background:'#f4f8fc',foreground:'#14263a',muted:'#607286',primary:'#2b8c8f',secondary:'#75a7c8',accent:'#2b8c8f',primaryText:'#ffffff',card:'#ffffff',border:'#d7e1eb',radius:16,cardOpacity:.88,borderOpacity:.55,borderWidth:1,shadow:'soft',backgroundMode:'color',gradientEnd:'#dcecf8',gradientAngle:135,sceneImageUrl:'',imageOverlay:.18,imageBlur:0}
const BUILTIN:any[]=[
 {id:'dock-green',name:'Dock Green',definition:{...baseDef,background:'#f6fbf8',primary:'#2b8c8f',secondary:'#79bfae',accent:'#56b89c',gradientEnd:'#dff4ea'}},
 {id:'slate',name:'Slate',definition:{...baseDef,background:'#eef2f6',primary:'#475569',secondary:'#64748b',accent:'#7c8aa0',gradientEnd:'#d5dde7'}},
 {id:'warm',name:'Warm',definition:{...baseDef,background:'#fbf4ea',primary:'#b86b3d',secondary:'#d39a6d',accent:'#c98252',gradientEnd:'#f3dfc9'}},
 {id:'sunset',name:'Sunset',definition:{...baseDef,background:'#fff1e7',primary:'#e87d64',secondary:'#8b72b7',accent:'#f2a56f',backgroundMode:'gradient',gradientEnd:'#e8ddfa'}},
 {id:'violet-harbor',name:'Violet Harbor',definition:{...baseDef,background:'#f4f0fa',primary:'#7d67aa',secondary:'#9d86bd',accent:'#6f72c9',backgroundMode:'gradient',gradientEnd:'#e3edf8'}},
 {id:'rubber-ducky',name:'Rubber Ducky',definition:{...baseDef,background:'#fff8c9',primary:'#21a7d7',secondary:'#ffe25f',accent:'#48bfdc',backgroundMode:'gradient',gradientEnd:'#c9f1ff'}},
 {id:'crazy-ducky',name:'Crazy Ducky',definition:{...baseDef,background:'#fff1fb',primary:'#ed4b9a',secondary:'#57d8ff',accent:'#f4d94f',backgroundMode:'gradient',gradientEnd:'#d9f6ff'}},
 {id:'tie-dye',name:'Tie Dye',definition:{...baseDef,background:'#fff2f8',primary:'#7c4dff',secondary:'#2bbec8',accent:'#ee5f9f',backgroundMode:'gradient',gradientEnd:'#dff5df'}},
 {id:'skipper-harbor',name:'Skipper Harbor',definition:{...baseDef,background:'#edf7fa',primary:'#2d6f94',secondary:'#5f89d9',accent:'#3fb8c7',backgroundMode:'gradient',gradientEnd:'#d6e8f5'}}
]
const blankOrg={name:'',org_code:'',email_domain:'',plan:'district',max_users:500,license_status:'trial',license_renewal_date:'',grace_period_days:30,minimum_extension_version:'',owner_notes:'',district_logo_url:'',district_background_url:'',district_accent_color:'#2b8c8f',district_primary_color:'#2b8c8f',district_secondary_color:'#75a7c8',default_theme:'',allow_user_theme_override:true,allow_admin_branding:true,branding_permissions:{logo:true,colors:true,background:true,theme:true},customer_contact_name:'',customer_contact_email:'',customer_contact_phone:'',technical_contact_name:'',technical_contact_email:'',technical_contact_phone:'',billing_contact_name:'',billing_contact_email:'',billing_contact_phone:'',trial_start_date:'',trial_end_date:'',billing_price_cents:'',billing_cadence:'annual',customer_lifecycle:'setup'}
const blankDistrict:DistrictForm={organization:{...blankOrg},domains:[],admins:[],allowedUsers:[]}
const blankTheme:ThemeDraft={name:'New Dock Theme',slug:'new-dock-theme',organizationId:'',definition:{...baseDef}}
const defaultSettings:any={default_trial_days:30,default_grace_days:30,default_plan:'district',default_seats:500,default_minimum_version:'',default_theme:'dock-green',allow_user_theme_override:true,allow_admin_branding:true,support_email:'',billing_email:'',support_url:'',notify_payment_failed:true,notify_trial_ending:true,notify_renewal:true,notify_suspension:true,notify_seat_threshold:true,notify_workspace_failure:true,notify_incident:true,notify_outdated_version:true,notify_new_admin:true,maintenance_mode:false,emergency_restrictions:false}
const eventNames:Record<string,string>={district_admin_publish:'Workspace published',district_admin_save_draft:'Workspace draft saved',owner_upsert_district:'District updated',owner_publish_theme:'Theme published',owner_save_theme:'Theme draft saved',owner_activate_user:'User activated',owner_deactivate_user:'User deactivated',owner_update_settings:'Owner settings updated',owner_save_release:'Release updated',owner_approve_release_preview:'Release preview approved',owner_approve_production_release:'Production release approved'}

function slugify(v:string){return String(v||'').trim().toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')}
function dateInput(v:any){if(!v)return '';const d=new Date(v);return Number.isNaN(d.getTime())?'':d.toISOString().slice(0,10)}
function toForm(item:any):DistrictForm{const o=item?.organization||{};return {organization:{...blankOrg,...o,license_renewal_date:dateInput(o.license_renewal_date),trial_start_date:dateInput(o.trial_start_date),trial_end_date:dateInput(o.trial_end_date),billing_price_cents:o.billing_price_cents??'',branding_permissions:{...blankOrg.branding_permissions,...(o.branding_permissions||{})}},domains:(item?.domains||[]).map((x:any)=>({...x,domain:x.domain||x.normalized_domain||''})),admins:item?.admins||[],allowedUsers:item?.allowedUsers||[]}}
function themeFromRecord(t:any):ThemeDraft{return {id:t.id,name:t.name||'Theme',slug:t.slug||slugify(t.name),organizationId:t.organization_id||'',definition:{...baseDef,...(t.definition||{})}}}
function hexToRgb(hex:string){const n=parseInt(String(hex).replace('#',''),16);return {r:(n>>16)&255,g:(n>>8)&255,b:n&255}}
function rgbToHex(r:number,g:number,b:number){return '#'+[r,g,b].map(v=>Math.max(0,Math.min(255,Math.round(v))).toString(16).padStart(2,'0')).join('')}
function mix(hex:string,target:string,p:number){const a=hexToRgb(hex),b=hexToRgb(target);return rgbToHex(a.r+(b.r-a.r)*p,a.g+(b.g-a.g)*p,a.b+(b.b-a.b)*p)}
function generateTheme(base:string,mood:string,name:string,orgId:string):ThemeDraft{const dark=mood==='dark',spirit=mood==='spirit',soft=mood==='soft';const bg=dark?mix(base,'#07111f',.78):soft?mix(base,'#ffffff',.92):spirit?mix(base,'#ffffff',.88):mix(base,'#ffffff',.95);return {name,slug:slugify(name),organizationId:orgId,definition:{...baseDef,background:bg,foreground:dark?'#f5f8fc':'#14263a',muted:dark?'#b3c2d2':'#607286',primary:base,secondary:mix(base,'#ffffff',.4),accent:spirit?mix(base,'#ffcc4d',.28):mix(base,'#42d3c4',.22),card:dark?mix(base,'#0b1726',.82):'#ffffff',border:dark?mix(base,'#ffffff',.64):mix(base,'#8ca0b4',.55),radius:spirit?20:soft?18:14,cardOpacity:dark?.82:.9,shadow:dark?'deep':'soft',backgroundMode:'gradient',gradientEnd:dark?mix(base,'#000000',.67):mix(base,'#ffffff',.72)}}}
async function fileData(file:File){if(!file.type.startsWith('image/'))throw new Error('Choose an image.');if(file.size>4*1024*1024)throw new Error('Keep images under 4 MB.');return await new Promise<string>((res,rej)=>{const r=new FileReader();r.onload=()=>res(String(r.result||''));r.onerror=()=>rej(new Error('Image read failed.'));r.readAsDataURL(file)})}
function daysUntil(v:any){if(!v)return null;const d=new Date(v);if(Number.isNaN(d.getTime()))return null;return Math.ceil((d.getTime()-Date.now())/86400000)}
function eventName(v:string){return eventNames[v]||String(v||'Activity').replace(/_/g,' ').replace(/\b\w/g,m=>m.toUpperCase())}
function fmtMoney(c:any){return c==null||c===''?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(c)/100)}

export default function HqV4(){
 const [user,setUser]=useState<any>(null),[districts,setDistricts]=useState<any[]>([]),[form,setForm]=useState<DistrictForm>(blankDistrict),[view,setView]=useState<View>('overview'),[selectedCode,setSelectedCode]=useState('')
 const [loading,setLoading]=useState(false),[status,setStatus]=useState('Dock HQ V4 ready.'),[dirty,setDirty]=useState(false),[themes,setThemes]=useState<any[]>([]),[theme,setTheme]=useState<ThemeDraft>(blankTheme),[themeStatus,setThemeStatus]=useState('Theme Studio ready.'),[userSearch,setUserSearch]=useState(''),[userFilter,setUserFilter]=useState('all'),[portfolioSearch,setPortfolioSearch]=useState(''),[portfolioStatus,setPortfolioStatus]=useState('all'),[activitySearch,setActivitySearch]=useState('')
 const [settings,setSettings]=useState<any>(defaultSettings),[settingsMeta,setSettingsMeta]=useState<any>({}),[releases,setReleases]=useState<any[]>([]),[releaseDraft,setReleaseDraft]=useState<any>({version:'',channel:'development',status:'draft',deployment_url:'',chrome_status:'',notes_internal:'',notes_public:'',build_verified:false,migrations_verified:false,managed_config_verified:false,theme_runtime_verified:false}),[versionCounts,setVersionCounts]=useState<any>({}),[diagnostics,setDiagnostics]=useState<any>(null),[diagEmail,setDiagEmail]=useState('')
 const editorRef=useRef<HTMLDivElement|null>(null)
 const selected=useMemo(()=>districts.find(d=>d.organization?.org_code===selectedCode),[districts,selectedCode])
 const totals=useMemo(()=>({districts:districts.length,seats:districts.reduce((s,d)=>s+(+d.organization?.max_users||0),0),used:districts.reduce((s,d)=>s+(+d.activeSeatCount||0),0),active:districts.filter(d=>d.organization?.license_status==='active').length}),[districts])
 const attentionItems=useMemo(()=>districts.flatMap(d=>{const a:any[]=[];const o=d.organization||{};if(['past_due','suspended','expired'].includes(o.license_status))a.push([d,'License needs attention',o.license_status,'licensing']);if(o.license_status==='trial'&&!o.trial_end_date)a.push([d,'Trial has no end date','Configure trial','licensing']);const due=daysUntil(o.license_renewal_date);if(due!==null&&due>=0&&due<=60)a.push([d,'Renewal approaching',`${due} days`,'licensing']);if(!d.domains?.some((x:any)=>x.status==='verified'))a.push([d,'No verified domain','Setup','districts']);if(!d.admins?.length)a.push([d,'No district admin','Setup','districts']);if(!d.publishedWorkspace)a.push([d,'No published workspace','Setup','workspaces']);return a}),[districts])
 const allActivity=useMemo(()=>districts.flatMap(d=>(d.auditLogs||[]).map((x:any)=>({...x,districtName:d.organization?.name,orgCode:d.organization?.org_code}))).sort((a:any,b:any)=>new Date(b.created_at).getTime()-new Date(a.created_at).getTime()),[districts])
 const allUsers=useMemo(()=>districts.flatMap(d=>(d.users||[]).map((u:any)=>({...u,districtName:d.organization?.name,orgCode:d.organization?.org_code,maxUsers:d.organization?.max_users}))),[districts])
 const visibleUsers=useMemo(()=>{const rows=selected?(selected.users||[]).map((u:any)=>({...u,districtName:selected.organization.name,orgCode:selected.organization.org_code})):allUsers;return rows.filter((u:any)=>{const text=`${u.email||''} ${u.role||''} ${u.districtName||''}`.toLowerCase();const match=!userSearch||text.includes(userSearch.toLowerCase());const filter=userFilter==='all'||(userFilter==='active'&&u.status!=='inactive')||(userFilter==='inactive'&&u.status==='inactive')||(userFilter==='admin'&&String(u.role).includes('admin'))||(userFilter==='never'&&!u.last_seen_at);return match&&filter})},[selected,allUsers,userSearch,userFilter])
 const filteredDistricts=useMemo(()=>districts.filter(d=>{const search=!portfolioSearch||`${d.organization?.name} ${d.organization?.email_domain} ${d.organization?.org_code}`.toLowerCase().includes(portfolioSearch.toLowerCase());const st=portfolioStatus==='all'||d.organization?.license_status===portfolioStatus;return search&&st}),[districts,portfolioSearch,portfolioStatus])
 const visibleActivity=useMemo(()=>allActivity.filter((a:any)=>!activitySearch||`${a.districtName} ${a.action} ${a.actor_email||''}`.toLowerCase().includes(activitySearch.toLowerCase())),[allActivity,activitySearch])
 const billingCounts=useMemo(()=>districts.reduce((m:any,d:any)=>{const s=d.organization?.license_status||'unknown';m[s]=(m[s]||0)+1;return m},{}),[districts])
 const currentRelease=useMemo(()=>releases.find(r=>r.status==='production')||releases[0]||null,[releases])

 useEffect(()=>{(async()=>{const u=await getUser();setUser(u);if(u)await Promise.all([loadDistricts(),loadThemes(),loadSettings(),loadReleases()])})()},[])
 async function authFetch(url:string,opts:any={}){const token=await getAccessToken();if(!token)throw new Error('Sign in again.');return fetch(url,{...opts,headers:{...(opts.headers||{}),Authorization:`Bearer ${token}`}})}
 async function loadDistricts(){try{setLoading(true);const r=await authFetch('/api/owner/districts',{cache:'no-store'}),j=await r.json();if(!r.ok)throw new Error(j.error||'Could not load districts.');setDistricts(j.districts||[]);setStatus(`Loaded ${j.districts?.length||0} districts.`)}catch(e:any){setStatus(e.message)}finally{setLoading(false)}}
 async function loadThemes(){try{const r=await authFetch('/api/owner/themes',{cache:'no-store'}),j=await r.json();if(!r.ok)throw new Error(j.error||'Could not load themes.');setThemes(j.themes||[])}catch(e:any){setThemeStatus(e.message)}}
 async function loadSettings(){try{const r=await authFetch('/api/owner/settings',{cache:'no-store'}),j=await r.json();if(!r.ok)throw new Error(j.error);setSettings(j.settings||defaultSettings);setSettingsMeta(j)}catch(e:any){setStatus(e.message)}}
 async function loadReleases(){try{const r=await authFetch('/api/owner/releases',{cache:'no-store'}),j=await r.json();if(!r.ok)throw new Error(j.error);setReleases(j.releases||[]);setVersionCounts(j.versionCounts||{})}catch(e:any){setStatus(e.message)}}
 async function saveDistrict(){try{setLoading(true);const r=await authFetch('/api/owner/districts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)}),j=await r.json();if(!r.ok)throw new Error(j.error||'Save failed.');setDistricts(j.districts||[]);const cur=(j.districts||[]).find((d:any)=>d.organization?.org_code===j.organization?.org_code);if(cur){setForm(toForm(cur));setSelectedCode(cur.organization.org_code)}setDirty(false);setStatus(`Saved ${j.organization?.name}.`)}catch(e:any){setStatus(e.message)}finally{setLoading(false)}}
 async function toggleUser(u:any){const district=districts.find(d=>d.organization?.org_code===u.orgCode)||selected;if(!district?.organization?.id)return;const next=u.status==='inactive'?'active':'inactive';if(!window.confirm(`${next==='inactive'?'Deactivate':'Activate'} ${u.email}?`))return;try{const r=await authFetch('/api/owner/users',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({organizationId:district.organization.id,userId:u.id,status:next})}),j=await r.json();if(!r.ok)throw new Error(j.error||'User update failed.');setDistricts(j.districts||[]);setStatus(`${u.email} is ${next}.`)}catch(e:any){setStatus(e.message)}}
 async function saveTheme(action:'save'|'publish'){try{setLoading(true);const r=await authFetch('/api/owner/themes',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,id:theme.id,name:theme.name,slug:theme.slug,organizationId:theme.organizationId||null,definition:theme.definition})}),j=await r.json();if(!r.ok)throw new Error(j.error||'Theme save failed.');setThemes(j.themes||[]);setTheme(themeFromRecord(j.theme));setThemeStatus(action==='publish'?'Theme published.':'Theme draft saved.');if(action==='publish')await loadDistricts()}catch(e:any){setThemeStatus(e.message)}finally{setLoading(false)}}
 async function saveSettings(){try{const r=await authFetch('/api/owner/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({settings})}),j=await r.json();if(!r.ok)throw new Error(j.error);setSettings(j.settings);setSettingsMeta(j);setStatus('Owner settings saved.')}catch(e:any){setStatus(e.message)}}
 async function saveRelease(action='save'){try{const r=await authFetch('/api/owner/releases',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...releaseDraft,action})}),j=await r.json();if(!r.ok)throw new Error(j.error);setReleases(j.releases||[]);setVersionCounts(j.versionCounts||{});setStatus(action==='save'?'Release saved.':action==='approve_preview'?'Preview approved.':'Production release approved.')}catch(e:any){setStatus(e.message)}}
 async function runDiagnostics(){try{const q=diagEmail?`?email=${encodeURIComponent(diagEmail)}`:'';const r=await authFetch(`/api/owner/diagnostics${q}`,{cache:'no-store'}),j=await r.json();if(!r.ok)throw new Error(j.error);setDiagnostics(j);setStatus('Diagnostics complete.')}catch(e:any){setStatus(e.message)}}
 function updateOrg(k:string,v:any){setForm(c=>({...c,organization:{...c.organization,[k]:v}}));setDirty(true)}
 function choose(d:any,next:View=view){if(dirty&&!window.confirm('You have unsaved owner changes. Leave anyway?'))return;setSelectedCode(d?.organization?.org_code||'');setForm(d?toForm(d):blankDistrict);setDirty(false);setView(next)}
 function newDistrict(){setSelectedCode('');setForm(blankDistrict);setDirty(true);setView('districts')}
 function addRow(kind:RowKind){const row=kind==='domains'?{domain:'',status:'pending',domain_type:'additional'}:kind==='admins'?{email:'',name:'',role:'district_admin',status:'active'}:{email:'',name:'',note:'',status:'active',expires_at:''};setForm(c=>({...c,[kind]:[...(c as any)[kind],row]}));setDirty(true)}
 function patchRow(kind:RowKind,i:number,k:string,v:any){setForm(c=>({...c,[kind]:(c as any)[kind].map((x:any,n:number)=>n===i?{...x,[k]:v}:x)}));setDirty(true)}
 function removeRow(kind:RowKind,i:number){setForm(c=>({...c,[kind]:(c as any)[kind].filter((_:any,n:number)=>n!==i)}));setDirty(true)}
 function patchTheme(k:string,v:any){setTheme(c=>({...c,definition:{...c.definition,[k]:v}}))}
 function useBuiltin(b:any){setTheme({name:`${b.name} Copy`,slug:slugify(`${b.name} copy`),organizationId:selected?.organization?.id||'',definition:{...b.definition}});setThemeStatus(`${b.name} loaded as an editable exact copy.`);setTimeout(()=>editorRef.current?.scrollIntoView({behavior:'smooth',block:'start'}),20)}
 function startThemeForDistrict(){const base=form.organization.district_primary_color||form.organization.district_accent_color||'#2b8c8f';setTheme(generateTheme(base,'clean',`${selected?.organization?.name||'Dock'} Custom`,selected?.organization?.id||''));setTimeout(()=>editorRef.current?.scrollIntoView({behavior:'smooth',block:'start'}),20)}
 function setAccountState(state:'suspended'|'active'|'archived'){if(!selected)return;const reason=window.prompt(`Reason for ${state} action:`);if(!reason)return;updateOrg(state==='archived'?'customer_lifecycle':'license_status',state==='archived'?'archived':state);setForm(c=>({...c,organization:{...c.organization,owner_notes:`${c.organization.owner_notes||''}\n${new Date().toLocaleString()} — ${state}: ${reason}`.trim()}}));setStatus(`Review and Save district to confirm ${state}.`)}

 if(!user)return <main className="hq2SignIn"><section className="hq2SignInCard"><div className="hq2Logo">D</div><div className="hq2Eyebrow" style={{color:'#9cc8ff',marginTop:20}}>Owner Control Plane</div><h1>Dock HQ V4</h1><p>Your private mothership for customers, licenses, users, themes, releases and operations.</p><button onClick={signIn}>Enter Dock HQ</button></section></main>
 const currentName=selected?.organization?.name||'All Districts'
 const selectedUsed=selected?.activeSeatCount||0,selectedMax=selected?.organization?.max_users||0
 const scopeUsed=selected?selectedUsed:totals.used,scopeMax=selected?selectedMax:totals.seats,scopePct=scopeMax?Math.round(scopeUsed/scopeMax*100):0
 const upcoming=districts.flatMap(d=>{const o=d.organization||{};const values=[['Trial ends',o.trial_end_date],['Renews',o.license_renewal_date]];return values.map(([label,date])=>({d,label,date,n:daysUntil(date)})).filter(x=>x.n!==null&&x.n!>=-30&&x.n!<=90)}).sort((a:any,b:any)=>(a.n??999)-(b.n??999))
 const workspaceHealthy=districts.filter(d=>d.publishedWorkspace).length
 const installedCount=Object.values(versionCounts).reduce((a:number,b:any)=>a+Number(b||0),0)
 const releaseReady=releaseDraft.build_verified&&releaseDraft.migrations_verified&&releaseDraft.managed_config_verified&&releaseDraft.theme_runtime_verified&&releaseDraft.notes_public

 return <div className="hq2Shell"><header className="hq2Top"><div className="hq2TopInner"><div className="hq2Brand"><div className="hq2Logo">D</div><div><strong>Dock HQ V4</strong><span>Owner mothership · {user.email}</span></div></div><div className="hq2TopActions"><select value={selectedCode} onChange={e=>{const d=districts.find(x=>x.organization.org_code===e.target.value);choose(d||null)}}><option value="">All Districts</option>{districts.map(d=><option key={d.organization.id} value={d.organization.org_code}>{d.organization.name}</option>)}</select><button onClick={()=>Promise.all([loadDistricts(),loadThemes(),loadSettings(),loadReleases()])}>{loading?'Working…':'Refresh'}</button><button onClick={signOut}>Sign out</button><button onClick={newDistrict}>+ New District</button></div></div></header>
 <div className="hq2App"><aside className="hq2Nav"><div className="hq2NavLabel">Dock HQ</div>{NAV.map(([id,label])=><button key={id} className={view===id?'active':''} onClick={()=>{if(dirty&&!window.confirm('You have unsaved owner changes. Leave anyway?'))return;setView(id)}}>{label}</button>)}<div className="hq2NavDivider"/><div className="hq2NavNote">Owner work stays here. District Admin remains customer-facing.</div></aside>
 <main className="hq2Content"><section className="hq2Hero"><div><div className="hq2Eyebrow">Dock Mothership</div><h1>{NAV.find(x=>x[0]===view)?.[1]}</h1><p>{view==='overview'?'Run the entire Dock business from one place.':`Selected: ${currentName}`}</p></div><span className="hq2Badge">Owner authenticated</span></section>

 {view==='overview'&&<><section className="hq2Metrics"><div className="hq2Metric"><span>Districts</span><strong>{totals.districts}</strong><small>customers</small></div><div className="hq2Metric"><span>Seats</span><strong>{totals.used}/{totals.seats}</strong><small>active / licensed</small></div><div className="hq2Metric"><span>Active</span><strong>{totals.active}</strong><small>paid/current</small></div><div className="hq2Metric"><span>Attention</span><strong>{attentionItems.length}</strong><small>items to review</small></div></section>
 <section className="hq2Grid2"><div className="hq2Card"><h2>Needs attention</h2><p style={{marginBottom:12}}>Owner actions that deserve review.</p>{attentionItems.length?attentionItems.slice(0,8).map((a:any,i:number)=><button className="hq2User hq2ActionRow" key={i} onClick={()=>choose(a[0],a[3])}><div><strong>{a[0].organization.name}</strong><div className="hq2Sub">{a[1]}</div></div><span className="hq2Status trial">{a[2]}</span></button>):<div className="hq2Empty">No urgent owner actions.</div>}</div><div className="hq2Card"><h2>System health</h2><div className="hq2Ops"><div className="hq2Op"><strong>Database</strong><span>Healthy · {districts.length} tenants loaded</span></div><div className="hq2Op"><strong>Authentication</strong><span>Owner authenticated</span></div><div className="hq2Op"><strong>Managed config</strong><span>{workspaceHealthy}/{districts.length} published</span></div><div className="hq2Op"><strong>Billing automation</strong><span>{districts.some(d=>d.billing)?'Subscription records detected':'Stripe not connected'}</span></div></div></div></section>
 <section className="hq2Grid2"><div className="hq2Card"><h2>Billing health</h2><div className="hq2Ops"><div className="hq2Op"><strong>Active</strong><span>{billingCounts.active||0}</span></div><div className="hq2Op"><strong>Trial</strong><span>{billingCounts.trial||0}</span></div><div className="hq2Op"><strong>Past due</strong><span>{billingCounts.past_due||0}</span></div><div className="hq2Op"><strong>Suspended / expired</strong><span>{(billingCounts.suspended||0)+(billingCounts.expired||0)}</span></div></div></div><div className="hq2Card"><h2>Release & extension health</h2><div className="hq2Ops"><div className="hq2Op"><strong>Production version</strong><span>{currentRelease?.version||'No production release recorded'}</span></div><div className="hq2Op"><strong>Minimum supported</strong><span>{settings.default_minimum_version||'Not set'}</span></div><div className="hq2Op"><strong>Install reports</strong><span>{installedCount}</span></div><div className="hq2Op"><strong>Managed workspaces</strong><span>{workspaceHealthy}/{districts.length}</span></div></div></div></section>
 <section className="hq2Grid2"><div className="hq2Card"><h2>Upcoming renewals & trials</h2>{upcoming.length?upcoming.slice(0,8).map((x:any,i:number)=><button className="hq2User hq2ActionRow" key={i} onClick={()=>choose(x.d,'licensing')}><div><strong>{x.d.organization.name}</strong><div className="hq2Sub">{x.label}</div></div><span>{x.n!<0?'Past due':`${x.n} days`}</span></button>):<div className="hq2Empty">No upcoming dates available. Trials without end dates appear in Needs Attention.</div>}</div><div className="hq2Card"><h2>Recent activity</h2>{allActivity.slice(0,6).map((a:any,i:number)=><div className="hq2User" key={i}><div><strong>{eventName(a.action)}</strong><div className="hq2Sub">{a.districtName} · {a.actor_email||'System / District Admin'}</div></div><span className="hq2Sub">{a.created_at?new Date(a.created_at).toLocaleString():''}</span></div>)}</div></section>
 <section className="hq2Card"><div className="hq2CardHeader"><div><h2>Customer portfolio</h2><p>Search and manage every tenant.</p></div><div className="hq2Inline"><input placeholder="Search districts" value={portfolioSearch} onChange={e=>setPortfolioSearch(e.target.value)}/><select value={portfolioStatus} onChange={e=>setPortfolioStatus(e.target.value)}><option value="all">All statuses</option>{['trial','active','past_due','suspended','expired'].map(x=><option key={x} value={x}>{x}</option>)}</select><button className="hq2Primary" onClick={newDistrict}>New district</button></div></div><table className="hq2Table"><thead><tr><th>District</th><th>License</th><th>Seats</th><th>Workspace</th><th/></tr></thead><tbody>{filteredDistricts.map(d=><tr key={d.organization.id}><td><div className="hq2DistrictName">{d.organization.name}</div><div className="hq2Sub">{d.organization.org_code} · {d.organization.email_domain}</div></td><td><span className={`hq2Status ${d.organization.license_status}`}>{d.organization.license_status}</span></td><td>{d.activeSeatCount||0}/{d.organization.max_users}</td><td>v{d.publishedWorkspace?.version||'—'}</td><td><button className="hq2Ghost" onClick={()=>choose(d,'districts')}>Manage</button></td></tr>)}</tbody></table></section></>}

 {view==='districts'&&(!selected&&!dirty?<section className="hq2Card"><div className="hq2CardHeader"><div><h2>District directory</h2><p>Select a customer to manage, or create a new district.</p></div><button className="hq2Primary" onClick={newDistrict}>New district</button></div><table className="hq2Table"><thead><tr><th>District</th><th>Lifecycle</th><th>License</th><th>Seats</th><th/></tr></thead><tbody>{districts.map(d=><tr key={d.organization.id}><td><strong>{d.organization.name}</strong><div className="hq2Sub">{d.organization.email_domain}</div></td><td>{d.organization.customer_lifecycle||'setup'}</td><td><span className={`hq2Status ${d.organization.license_status}`}>{d.organization.license_status}</span></td><td>{d.activeSeatCount||0}/{d.organization.max_users}</td><td><button className="hq2Ghost" onClick={()=>choose(d,'districts')}>Manage</button></td></tr>)}</tbody></table></section>:<><section className="hq2Card"><div className="hq2CardHeader"><div><h2>{form.organization.name||'New district'}</h2><p>{form.organization.org_code||'new-org'} · {form.organization.email_domain||'no domain'} · {form.organization.license_status||'trial'} · {selected?.activeSeatCount||0}/{form.organization.max_users||0} seats</p></div><button className="hq2Primary" onClick={saveDistrict}>Save district</button></div><div className="hq2FormGrid">{[['District name','name'],['Org code','org_code'],['Primary domain','email_domain'],['Primary contact','customer_contact_name'],['Contact email','customer_contact_email'],['Contact phone','customer_contact_phone'],['Technical contact','technical_contact_name'],['Technical email','technical_contact_email'],['Billing contact','billing_contact_name'],['Billing email','billing_contact_email']].map(([l,k])=><div className="hq2Field" key={k}><label>{l}</label><input disabled={k==='org_code'&&!!selected} value={form.organization[k]||''} onChange={e=>updateOrg(k,k==='org_code'?slugify(e.target.value):e.target.value)}/></div>)}<div className="hq2Field"><label>Customer lifecycle</label><select value={form.organization.customer_lifecycle||'setup'} onChange={e=>updateOrg('customer_lifecycle',e.target.value)}>{['lead','setup','trial','active','offboarding','archived'].map(x=><option key={x}>{x}</option>)}</select></div><div className="hq2Field full"><label>Owner notes</label><textarea value={form.organization.owner_notes||''} onChange={e=>updateOrg('owner_notes',e.target.value)}/></div></div></section>
 <section className="hq2Grid2"><div className="hq2Card"><h2>Onboarding checklist</h2>{[['Customer record',!!selected],['Primary domain',!!form.organization.email_domain],['Domain verified',!!form.domains.some(x=>x.status==='verified')],['District admin assigned',!!form.admins.length],['Branding configured',!!(form.organization.district_logo_url||form.organization.district_primary_color)],['Theme assigned',!!selected&&!!form.organization.default_theme],['Workspace published',!!selected?.publishedWorkspace],['License activated',form.organization.license_status==='active'],['Billing connected',!!selected?.billing]].map(([l,ok]:any)=><div className="hq2User" key={l}><strong>{l}</strong><span>{ok?'✓':'○'}</span></div>)}</div><div className="hq2Card"><h2>Quick actions</h2><div className="hq2Inline">{selected&&([['License','licensing'],['Users','users'],['Theme','themes'],['Branding','branding'],['Workspace','workspaces']] as any[]).map(([l,v])=><button className="hq2Ghost" key={v} onClick={()=>setView(v)}>{l}</button>)}</div><div className="hq2Notice">Organization code is locked after a district is created. Customer lifecycle is separate from license status.</div></div></section>
 <section className="hq2Card"><h2>Domains & delegated access</h2><h3 className="hq2SectionTitle">Verified domains</h3>{form.domains.length?form.domains.map((x:any,i:number)=><div className="hq2MiniRow" key={i}><input value={x.domain||''} onChange={e=>patchRow('domains',i,'domain',e.target.value)}/><select value={x.status||'pending'} onChange={e=>patchRow('domains',i,'status',e.target.value)}><option>verified</option><option>pending</option></select><button className="hq2Danger" onClick={()=>removeRow('domains',i)}>Remove</button></div>):<div className="hq2Empty">No domains configured.</div>}<button className="hq2Ghost" onClick={()=>addRow('domains')}>Add domain</button><h3 className="hq2SectionTitle">District admins</h3>{form.admins.length?form.admins.map((x:any,i:number)=><div className="hq2MiniRow admin" key={i}><input placeholder="email" value={x.email||''} onChange={e=>patchRow('admins',i,'email',e.target.value)}/><select value={x.status||'active'} onChange={e=>patchRow('admins',i,'status',e.target.value)}><option value="active">Active</option><option value="disabled">Disabled</option></select><button className="hq2Danger" onClick={()=>removeRow('admins',i)}>Remove</button></div>):<div className="hq2Empty">No district administrators assigned.</div>}<button className="hq2Ghost" onClick={()=>addRow('admins')}>Add admin</button><h3 className="hq2SectionTitle">Outside-domain users</h3>{form.allowedUsers.length?form.allowedUsers.map((x:any,i:number)=><div className="hq2MiniRow allowed" key={i}><input placeholder="email" value={x.email||''} onChange={e=>patchRow('allowedUsers',i,'email',e.target.value)}/><input placeholder="name" value={x.name||''} onChange={e=>patchRow('allowedUsers',i,'name',e.target.value)}/><input placeholder="reason" value={x.note||''} onChange={e=>patchRow('allowedUsers',i,'note',e.target.value)}/><button className="hq2Danger" onClick={()=>removeRow('allowedUsers',i)}>Remove</button></div>):<div className="hq2Empty">No outside-domain exceptions.</div>}<button className="hq2Ghost" onClick={()=>addRow('allowedUsers')}>Add user</button></section>
 {selected&&<section className="hq2Card"><h2>Account actions</h2><p style={{marginBottom:12}}>High-impact changes require a reason and are only persisted when you Save district.</p><div className="hq2Inline"><button className="hq2Danger" onClick={()=>setAccountState('suspended')}>Suspend customer</button><button className="hq2Ghost" onClick={()=>setAccountState('archived')}>Archive customer</button><button className="hq2Ghost" onClick={()=>setAccountState('active')}>Reactivate</button></div></section>}</>)}

 {view==='licensing'&&(!selected?<section className="hq2Card"><h2>Licensing portfolio</h2><p style={{marginBottom:14}}>Choose a district to edit billing and enforcement.</p><table className="hq2Table"><thead><tr><th>District</th><th>Status</th><th>Seats</th><th>Billing</th><th/></tr></thead><tbody>{districts.map(d=><tr key={d.organization.id}><td>{d.organization.name}</td><td>{d.organization.license_status}</td><td>{d.activeSeatCount||0}/{d.organization.max_users}</td><td>{d.billing?.status||'Not connected'}</td><td><button className="hq2Ghost" onClick={()=>choose(d,'licensing')}>Open</button></td></tr>)}</tbody></table></section>:<><section className="hq2Card"><div className="hq2CardHeader"><div><h2>{currentName} license & billing</h2><p>Payment truth and Dock access truth in one place.</p></div><button className="hq2Primary" onClick={saveDistrict}>Save license</button></div><div className="hq2FormGrid"><div className="hq2Field"><label>Plan</label><select value={form.organization.plan} onChange={e=>updateOrg('plan',e.target.value)}><option value="district">District</option><option value="school">School</option><option value="pilot">Pilot</option></select></div><div className="hq2Field"><label>Status</label><select value={form.organization.license_status} onChange={e=>updateOrg('license_status',e.target.value)}>{['trial','active','past_due','suspended','expired'].map(x=><option key={x}>{x}</option>)}</select></div><div className="hq2Field"><label>Seats</label><input type="number" value={form.organization.max_users} onChange={e=>updateOrg('max_users',+e.target.value)}/></div><div className="hq2Field"><label>Seats used</label><input disabled value={selectedUsed}/></div><div className="hq2Field"><label>Trial start</label><input type="date" value={form.organization.trial_start_date||''} onChange={e=>updateOrg('trial_start_date',e.target.value)}/></div><div className="hq2Field"><label>Trial end</label><input type="date" value={form.organization.trial_end_date||''} onChange={e=>updateOrg('trial_end_date',e.target.value)}/></div><div className="hq2Field"><label>Renewal date</label><input type="date" value={form.organization.license_renewal_date||''} onChange={e=>updateOrg('license_renewal_date',e.target.value)}/></div><div className="hq2Field"><label>Grace days</label><input type="number" value={form.organization.grace_period_days} onChange={e=>updateOrg('grace_period_days',+e.target.value)}/></div><div className="hq2Field"><label>Contract price</label><input type="number" value={form.organization.billing_price_cents||''} onChange={e=>updateOrg('billing_price_cents',e.target.value===''?'':Math.round(+e.target.value))} placeholder="cents"/></div><div className="hq2Field"><label>Cadence</label><select value={form.organization.billing_cadence||'annual'} onChange={e=>updateOrg('billing_cadence',e.target.value)}><option>monthly</option><option>annual</option><option>custom</option></select></div><div className="hq2Field"><label>Minimum extension version</label><input value={form.organization.minimum_extension_version||''} onChange={e=>updateOrg('minimum_extension_version',e.target.value)}/></div></div></section><section className="hq2Grid2"><div className="hq2Card"><h2>Dock enforcement</h2><div className="hq2Ops"><div className="hq2Op"><strong>Access result</strong><span>{['suspended','expired'].includes(form.organization.license_status)?'ACCESS BLOCKED':form.organization.license_status==='past_due'?'GRACE PERIOD':'ACCESS ALLOWED'}</span></div><div className="hq2Op"><strong>Seats</strong><span>{selectedUsed}/{selectedMax} · {scopePct}%</span></div></div></div><div className="hq2Card"><h2>Stripe / subscription</h2><div className="hq2Ops"><div className="hq2Op"><strong>Connection</strong><span>{selected.billing?'Connected':'Not connected'}</span></div><div className="hq2Op"><strong>Subscription</strong><span>{selected.billing?.status||'Unconfigured'}</span></div><div className="hq2Op"><strong>Last invoice</strong><span>{selected.billing?.last_invoice_status||'—'}</span></div><div className="hq2Op"><strong>Period end</strong><span>{selected.billing?.current_period_end?new Date(selected.billing.current_period_end).toLocaleDateString():'—'}</span></div></div></div></section><section className="hq2Card"><h2>License timeline</h2>{selected.auditLogs?.filter((a:any)=>String(a.action).includes('license')||String(a.action).includes('billing')).length?selected.auditLogs.filter((a:any)=>String(a.action).includes('license')||String(a.action).includes('billing')).map((a:any,i:number)=><div className="hq2User" key={i}><strong>{eventName(a.action)}</strong><span className="hq2Sub">{new Date(a.created_at).toLocaleString()}</span></div>):<div className="hq2Empty">No billing or license transition events recorded yet.</div>}</section></>)}

 {view==='users'&&<><section className="hq2Metrics"><div className="hq2Metric"><span>Used seats</span><strong>{scopeUsed}</strong><small>active profiles</small></div><div className="hq2Metric"><span>Licensed</span><strong>{scopeMax}</strong><small>available capacity</small></div><div className="hq2Metric"><span>Available</span><strong>{Math.max(0,scopeMax-scopeUsed)}</strong><small>{scopePct}% utilized</small></div><div className="hq2Metric"><span>Admins</span><strong>{selected?(selected.admins?.length||0):districts.reduce((n,d)=>n+(d.admins?.length||0),0)}</strong><small>district admins</small></div></section><section className="hq2Card"><div className="hq2CardHeader"><div><h2>{currentName} users & seats</h2><p>Search, inspect and reclaim seats without deleting history.</p></div><div style={{minWidth:180}}><div className="hq2Progress"><span style={{width:`${Math.min(100,scopePct)}%`}}/></div><div className="hq2Sub">{scopeUsed}/{scopeMax} · {scopePct}% used</div></div></div><div className="hq2Inline" style={{marginBottom:14}}><input placeholder="Search users" value={userSearch} onChange={e=>setUserSearch(e.target.value)}/><select value={userFilter} onChange={e=>setUserFilter(e.target.value)}><option value="all">All users</option><option value="active">Active</option><option value="inactive">Inactive</option><option value="admin">Admins</option><option value="never">Never seen</option></select></div>{visibleUsers.length?<table className="hq2Table"><thead><tr><th>User</th>{!selected&&<th>District</th>}<th>Role</th><th>Last seen</th><th>Status</th><th/></tr></thead><tbody>{visibleUsers.map((u:any)=><tr key={`${u.orgCode}-${u.id}`}><td>{u.email||'Unnamed'}</td>{!selected&&<td>{u.districtName}</td>}<td>{u.role||'member'}</td><td>{u.last_seen_at?new Date(u.last_seen_at).toLocaleString():'Never'}</td><td><span className={`hq2Status ${u.status==='inactive'?'expired':'active'}`}>{u.status||'active'}</span></td><td><button className={u.status==='inactive'?'hq2Primary':'hq2Danger'} onClick={()=>toggleUser(u)}>{u.status==='inactive'?'Activate':'Deactivate'}</button></td></tr>)}</tbody></table>:<div className="hq2Empty">No matching user profiles.</div>}</section></>}

 {view==='themes'&&<><section className="hq2Card"><div className="hq2CardHeader"><div><h2>Theme library</h2><p>Preview built-ins, duplicate exact designs, and open custom themes.</p></div><button className="hq2Primary" onClick={startThemeForDistrict}>Create theme</button></div><div className="hq2ThemeGrid">{BUILTIN.map(b=><button className="hq2Theme" key={b.id} onClick={()=>useBuiltin(b)}><div className="hq2Swatch" style={{background:`linear-gradient(135deg,${b.definition.background},${b.definition.primary})`}}/><strong>{b.name}</strong><small>Built-in · click to duplicate & edit</small></button>)}{themes.filter(t=>t.status!=='archived').map(t=><button className={`hq2Theme ${theme.id===t.id?'selected':''}`} key={t.id} onClick={()=>{setTheme(themeFromRecord(t));setTimeout(()=>editorRef.current?.scrollIntoView({behavior:'smooth',block:'start'}),20)}}><div className="hq2Swatch" style={{background:`linear-gradient(135deg,${t.definition?.background||'#eef3f8'},${t.definition?.primary||'#2b8c8f'})`}}/><strong>{t.name}</strong><small>{t.status} · v{t.version} · {t.scope}</small></button>)}</div></section>
 <section className="hq2Card" ref={editorRef}><div className="hq2CardHeader"><div><h2>Theme editor</h2><p>{theme.id?`Editing ${theme.name}`:'Create, refine and preview a Dock theme.'}</p></div><div className="hq2Inline">{['clean','soft','spirit','dark'].map(m=><button className="hq2Ghost" key={m} onClick={()=>setTheme(generateTheme(form.organization.district_primary_color||'#2b8c8f',m,`${currentName} ${m}`,selected?.organization?.id||''))}>Generate {m}</button>)}<button className="hq2Ghost" onClick={()=>{const moods=['clean','soft','spirit','dark'];const m=moods[Math.floor(Math.random()*moods.length)];setTheme(generateTheme(form.organization.district_primary_color||'#2b8c8f',m,`${currentName} Surprise`,selected?.organization?.id||''))}}>Surprise me</button></div></div><div className="hq2Grid2"><div><div className="hq2FormGrid"><div className="hq2Field"><label>Theme name</label><input value={theme.name} onChange={e=>setTheme(c=>({...c,name:e.target.value,slug:slugify(e.target.value)}))}/></div><div className="hq2Field"><label>Assign to</label><select value={theme.organizationId} onChange={e=>setTheme(c=>({...c,organizationId:e.target.value}))}><option value="">Global library</option>{districts.map(d=><option key={d.organization.id} value={d.organization.id}>{d.organization.name}</option>)}</select></div>{[['Background','background'],['Primary','primary'],['Secondary','secondary'],['Accent','accent'],['Text','foreground'],['Muted','muted'],['Cards','card'],['Borders','border']].map(([l,k])=><div className="hq2Field" key={k}><label>{l}</label><div className="hq2Inline"><input type="color" value={theme.definition[k]} onChange={e=>patchTheme(k,e.target.value)}/><input value={theme.definition[k]} onChange={e=>patchTheme(k,e.target.value)}/></div></div>)}<div className="hq2Field"><label>Background mode</label><select value={theme.definition.backgroundMode} onChange={e=>patchTheme('backgroundMode',e.target.value)}><option value="color">Color</option><option value="gradient">Gradient</option><option value="image">Image</option></select></div><div className="hq2Field"><label>Gradient end</label><div className="hq2Inline"><input type="color" value={theme.definition.gradientEnd} onChange={e=>patchTheme('gradientEnd',e.target.value)}/><input value={theme.definition.gradientEnd} onChange={e=>patchTheme('gradientEnd',e.target.value)}/></div></div><div className="hq2Field"><label>Gradient angle · {theme.definition.gradientAngle}°</label><input type="range" min="0" max="360" value={theme.definition.gradientAngle} onChange={e=>patchTheme('gradientAngle',+e.target.value)}/></div><div className="hq2Field"><label>Corner radius · {theme.definition.radius}px</label><input type="range" min="4" max="28" value={theme.definition.radius} onChange={e=>patchTheme('radius',+e.target.value)}/></div><div className="hq2Field"><label>Card opacity · {Math.round(theme.definition.cardOpacity*100)}%</label><input type="range" min="45" max="100" value={Math.round(theme.definition.cardOpacity*100)} onChange={e=>patchTheme('cardOpacity',+e.target.value/100)}/></div><div className="hq2Field"><label>Shadow</label><select value={theme.definition.shadow} onChange={e=>patchTheme('shadow',e.target.value)}><option>none</option><option>soft</option><option>medium</option><option>deep</option></select></div><div className="hq2Field full"><label>Background image</label><input type="file" accept="image/*" onChange={async e=>{const f=e.target.files?.[0];if(f){patchTheme('sceneImageUrl',await fileData(f));patchTheme('backgroundMode','image')}}}/></div></div><div className="hq2Inline"><button className="hq2Ghost" onClick={()=>saveTheme('save')}>Save Draft</button><button className="hq2Primary" onClick={()=>saveTheme('publish')}>Publish Theme</button></div><div className="hq2Notice">{themeStatus}</div></div><div className="hq2Card" style={{position:'sticky',top:110}}><h2>Live Dock Preview</h2><div style={{borderRadius:theme.definition.radius,background:theme.definition.backgroundMode==='gradient'?`linear-gradient(${theme.definition.gradientAngle}deg,${theme.definition.background},${theme.definition.gradientEnd})`:theme.definition.backgroundMode==='image'&&theme.definition.sceneImageUrl?`linear-gradient(rgba(0,0,0,${theme.definition.imageOverlay}),rgba(0,0,0,${theme.definition.imageOverlay})),url(${theme.definition.sceneImageUrl}) center/cover`:theme.definition.background,padding:18,color:theme.definition.foreground,minHeight:360}}><div style={{display:'flex',justifyContent:'space-between'}}><strong>Dock</strong><span style={{color:theme.definition.muted}}>Welcome back</span></div><input placeholder="Search Dock" style={{width:'100%',margin:'14px 0',padding:10,borderRadius:theme.definition.radius,border:`1px solid ${theme.definition.border}`}}/><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10}}>{['Gmail','PowerSchool','Canvas','Memories'].map(x=><div key={x} style={{background:theme.definition.card,opacity:theme.definition.cardOpacity,border:`${theme.definition.borderWidth}px solid ${theme.definition.border}`,borderRadius:theme.definition.radius,padding:14,boxShadow:theme.definition.shadow==='none'?'none':'0 8px 24px rgba(0,0,0,.12)'}}><strong>{x}</strong><div style={{color:theme.definition.muted,fontSize:12,marginTop:4}}>Dock card preview</div><button style={{marginTop:12,background:theme.definition.primary,color:theme.definition.primaryText,border:0,borderRadius:10,padding:'7px 10px'}}>Open</button></div>)}</div></div></div></div></section></>}

 {view==='branding'&&(!selected?<section className="hq2Card"><h2>Branding portfolio</h2><p style={{marginBottom:14}}>Branding is district-specific. Choose a district to edit identity and assets.</p><div className="hq2ThemeGrid">{districts.map(d=><button className="hq2Theme" key={d.organization.id} onClick={()=>choose(d,'branding')}><div className="hq2Swatch" style={{background:d.organization.district_primary_color||d.organization.district_accent_color||'#2b8c8f'}}/>{d.organization.district_logo_url&&<img src={d.organization.district_logo_url} alt="" style={{width:38,height:38,objectFit:'contain'}}/>}<strong>{d.organization.name}</strong><small>{d.organization.default_theme||'No theme assigned'}</small></button>)}</div></section>:<section className="hq2Card"><div className="hq2CardHeader"><div><h2>{currentName} branding</h2><p>Identity, assets and delegation policy.</p></div><button className="hq2Primary" onClick={saveDistrict}>Save branding</button></div><div className="hq2Grid2"><div className="hq2FormGrid">{[['Primary','district_primary_color'],['Secondary','district_secondary_color'],['Accent','district_accent_color']].map(([l,k])=><div className="hq2Field" key={k}><label>{l}</label><div className="hq2Inline"><input type="color" value={form.organization[k]||'#2b8c8f'} onChange={e=>updateOrg(k,e.target.value)}/><input value={form.organization[k]||''} onChange={e=>updateOrg(k,e.target.value)}/></div></div>)}<div className="hq2Field"><label>Default theme</label><select value={form.organization.default_theme||''} onChange={e=>updateOrg('default_theme',e.target.value)}><option value="">Select theme</option>{BUILTIN.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}{themes.filter(t=>t.status==='published').map(t=><option key={t.id} value={t.slug}>{t.name}</option>)}</select></div><div className="hq2Field"><label>District logo</label>{form.organization.district_logo_url&&<div className="hq2Asset"><img src={form.organization.district_logo_url} alt="logo"/><button className="hq2Danger" onClick={()=>updateOrg('district_logo_url','')}>Remove</button></div>}<input type="file" accept="image/*" onChange={async e=>{const f=e.target.files?.[0];if(f)updateOrg('district_logo_url',await fileData(f))}}/></div><div className="hq2Field"><label>Background asset</label>{form.organization.district_background_url&&<div className="hq2Asset"><img src={form.organization.district_background_url} alt="background"/><button className="hq2Danger" onClick={()=>updateOrg('district_background_url','')}>Remove</button></div>}<input type="file" accept="image/*" onChange={async e=>{const f=e.target.files?.[0];if(f)updateOrg('district_background_url',await fileData(f))}}/></div><label className="hq2Check"><input type="checkbox" checked={form.organization.allow_user_theme_override!==false} onChange={e=>updateOrg('allow_user_theme_override',e.target.checked)}/> Users may personalize theme</label><label className="hq2Check"><input type="checkbox" checked={form.organization.allow_admin_branding!==false} onChange={e=>updateOrg('allow_admin_branding',e.target.checked)}/> District Admin may edit branding</label>{['logo','colors','background','theme'].map(k=><label className="hq2Check" key={k}><input type="checkbox" checked={form.organization.branding_permissions?.[k]!==false} onChange={e=>updateOrg('branding_permissions',{...form.organization.branding_permissions,[k]:e.target.checked})}/> Admin can edit {k}</label>)}</div><div className="hq2Preview" style={{backgroundColor:form.organization.district_primary_color||'#2b8c8f',backgroundImage:form.organization.district_background_url?`linear-gradient(rgba(10,28,48,.25),rgba(10,28,48,.25)),url(${form.organization.district_background_url})`:undefined}}>{form.organization.district_logo_url?<img src={form.organization.district_logo_url} alt="logo"/>:null}<strong>{currentName}</strong><span>{form.organization.default_theme||'No theme'} · managed Dock</span></div></div></section>)}

 {view==='workspaces'&&(!selected?<section className="hq2Card"><h2>Workspace portfolio</h2><p style={{marginBottom:14}}>Owner oversight across every district.</p><table className="hq2Table"><thead><tr><th>District</th><th>Live</th><th>Resources</th><th>Last published</th><th>Health</th><th/></tr></thead><tbody>{districts.map(d=><tr key={d.organization.id}><td>{d.organization.name}</td><td>v{d.publishedWorkspace?.version||'—'}</td><td>{d.workspaceTabs?.length||0}</td><td>{d.publishedWorkspace?.published_at?new Date(d.publishedWorkspace.published_at).toLocaleString():'—'}</td><td>{d.publishedWorkspace?'Healthy':'Needs setup'}</td><td><button className="hq2Ghost" onClick={()=>choose(d,'workspaces')}>Inspect</button></td></tr>)}</tbody></table></section>:<><section className="hq2Card"><div className="hq2CardHeader"><div><h2>{currentName} workspace</h2><p>Owner oversight, version history and recovery.</p></div><div className="hq2Inline"><a className="hq2Ghost" href={`/district/${encodeURIComponent(selectedCode)}`} target="_blank">Preview Live</a><a className="hq2Ghost" href="/admin" target="_blank">Open District Admin</a></div></div><div className="hq2Ops"><div className="hq2Op"><strong>Live version</strong><span>v{selected.publishedWorkspace?.version||'—'}</span></div><div className="hq2Op"><strong>Workspace</strong><span>{selected.publishedWorkspace?.name||'None'}</span></div><div className="hq2Op"><strong>Published</strong><span>{selected.publishedWorkspace?.published_at?new Date(selected.publishedWorkspace.published_at).toLocaleString():'—'}</span></div><div className="hq2Op"><strong>Resources</strong><span>{selected.workspaceTabs?.length||0} managed links</span></div></div></section><section className="hq2Grid2"><div className="hq2Card"><h2>Resources</h2>{selected.workspaceTabs?.length?selected.workspaceTabs.map((t:any)=><div className="hq2User" key={t.id}><div><strong>{t.title}</strong><div className="hq2Sub">{t.url}</div></div><span>{t.is_locked?'Locked':'Editable'}</span></div>):<div className="hq2Empty">No workspace resources found.</div>}</div><div className="hq2Card"><h2>Publishing health</h2><div className="hq2Ops"><div className="hq2Op"><strong>Published workspace</strong><span>{selected.publishedWorkspace?'Healthy':'Missing'}</span></div><div className="hq2Op"><strong>Resource payload</strong><span>{selected.workspaceTabs?.length?'Loaded':'Empty'}</span></div></div></div></section><section className="hq2Card"><h2>Version history</h2>{selected.workspaceVersions?.length?selected.workspaceVersions.map((v:any)=><div className="hq2User" key={`${v.version}-${v.created_at}`}><div><strong>v{v.version} · {v.name}</strong><div className="hq2Sub">{v.published_at?new Date(v.published_at).toLocaleString():''}</div></div><span className="hq2Sub">Snapshot retained</span></div>):<div className="hq2Empty">No version history.</div>}<div className="hq2Notice">Recovery snapshots are retained. Destructive rollback controls stay unavailable until the server-side restore transaction is implemented and tested.</div></section></>)}

 {view==='releases'&&<><section className="hq2Grid2"><div className="hq2Card"><h2>Release control</h2><div className="hq2Ops"><div className="hq2Op"><strong>Current production</strong><span>{currentRelease?.version||'No release recorded'}</span></div><div className="hq2Op"><strong>Global minimum</strong><span>{settings.default_minimum_version||'Not set'}</span></div><div className="hq2Op"><strong>Install reports</strong><span>{installedCount}</span></div><div className="hq2Op"><strong>Managed config</strong><span>{workspaceHealthy}/{districts.length} districts published</span></div></div></div><div className="hq2Card"><h2>Version adoption</h2>{Object.keys(versionCounts).length?Object.entries(versionCounts).map(([v,n]:any)=><div className="hq2User" key={v}><strong>{v}</strong><span>{n} installs</span></div>):<div className="hq2Empty">No extension version telemetry reported yet.</div>}</div></section><section className="hq2Card"><div className="hq2CardHeader"><div><h2>Prepare release</h2><p>Checks only show complete when you explicitly verify them.</p></div><button className="hq2Primary" onClick={()=>saveRelease('save')}>Save release</button></div><div className="hq2FormGrid"><div className="hq2Field"><label>Version</label><input value={releaseDraft.version} onChange={e=>setReleaseDraft((c:any)=>({...c,version:e.target.value}))}/></div><div className="hq2Field"><label>Channel</label><select value={releaseDraft.channel} onChange={e=>setReleaseDraft((c:any)=>({...c,channel:e.target.value}))}><option>development</option><option>pilot</option><option>beta</option><option>production</option></select></div><div className="hq2Field"><label>Deployment URL</label><input value={releaseDraft.deployment_url} onChange={e=>setReleaseDraft((c:any)=>({...c,deployment_url:e.target.value}))}/></div><div className="hq2Field"><label>Chrome status</label><input value={releaseDraft.chrome_status} onChange={e=>setReleaseDraft((c:any)=>({...c,chrome_status:e.target.value}))}/></div><div className="hq2Field full"><label>Internal notes</label><textarea value={releaseDraft.notes_internal} onChange={e=>setReleaseDraft((c:any)=>({...c,notes_internal:e.target.value}))}/></div><div className="hq2Field full"><label>Customer release notes</label><textarea value={releaseDraft.notes_public} onChange={e=>setReleaseDraft((c:any)=>({...c,notes_public:e.target.value}))}/></div></div><h3 className="hq2SectionTitle">Release readiness</h3>{[['Build verified','build_verified'],['Database migrations verified','migrations_verified'],['Managed config compatible','managed_config_verified'],['Theme runtime compatible','theme_runtime_verified']].map(([l,k])=><label className="hq2User" key={k}><strong>{l}</strong><input type="checkbox" checked={!!releaseDraft[k]} onChange={e=>setReleaseDraft((c:any)=>({...c,[k]:e.target.checked}))}/></label>)}<div className="hq2Inline" style={{marginTop:14}}><button className="hq2Ghost" disabled={!releaseDraft.version} onClick={()=>saveRelease('approve_preview')}>Approve preview</button><button className="hq2Primary" disabled={!releaseReady||!releaseDraft.version} onClick={()=>saveRelease('approve_production')}>Approve Production Release</button></div></section><section className="hq2Card"><h2>Release history</h2>{releases.length?releases.map(r=><button className="hq2User hq2ActionRow" key={r.id} onClick={()=>setReleaseDraft({...releaseDraft,...r})}><div><strong>{r.version} · {r.channel}</strong><div className="hq2Sub">{r.status} · {r.released_at?new Date(r.released_at).toLocaleString():'not released'}</div></div><span>{r.chrome_status||'Chrome status unknown'}</span></button>):<div className="hq2Empty">No releases recorded yet.</div>}</section></>}

 {view==='diagnostics'&&<><section className="hq2Card"><div className="hq2CardHeader"><div><h2>System health</h2><p>Run real owner-level checks without exposing secrets.</p></div><button className="hq2Primary" onClick={runDiagnostics}>Run Diagnostics</button></div><div className="hq2Inline" style={{marginBottom:14}}><input placeholder="Test a user email" value={diagEmail} onChange={e=>setDiagEmail(e.target.value)}/><button className="hq2Ghost" onClick={runDiagnostics}>Resolve access</button></div>{diagnostics?<div className="hq2Ops"><div className="hq2Op"><strong>Database</strong><span>{diagnostics.report.database}</span></div><div className="hq2Op"><strong>Authentication</strong><span>{diagnostics.report.authentication}</span></div><div className="hq2Op"><strong>Published workspaces</strong><span>{diagnostics.report.workspaceHealthy}/{diagnostics.report.districtCount}</span></div><div className="hq2Op"><strong>Verified domains</strong><span>{diagnostics.report.verifiedDomains}/{diagnostics.report.districtCount}</span></div><div className="hq2Op"><strong>Billing records</strong><span>{diagnostics.report.billingConnected}</span></div><div className="hq2Op"><strong>Extension reports</strong><span>{diagnostics.report.installationReports}</span></div></div>:<div className="hq2Empty">Run Diagnostics to collect current state.</div>}{diagnostics?.report?.resolution&&<div className="hq2Notice"><strong>Resolution:</strong> {diagnostics.report.resolution.matched?`${diagnostics.report.resolution.district} · ${diagnostics.report.resolution.access}`:'No matching district'}.</div>}</section><section className="hq2Card"><div className="hq2CardHeader"><div><h2>Support report</h2><p>Safe non-secret diagnostic state for support.</p></div>{diagnostics&&<button className="hq2Ghost" onClick={()=>navigator.clipboard.writeText(JSON.stringify(diagnostics.report,null,2))}>Copy Support Report</button>}</div>{diagnostics?<pre className="hq2Report">{JSON.stringify(diagnostics.report,null,2)}</pre>:<div className="hq2Empty">Run diagnostics first.</div>}</section></>}

 {view==='activity'&&<section className="hq2Card"><div className="hq2CardHeader"><div><h2>Audit & activity</h2><p>Who changed what, when, and for which district.</p></div><input placeholder="Search activity" value={activitySearch} onChange={e=>setActivitySearch(e.target.value)}/></div>{visibleActivity.length?visibleActivity.map((a:any,i:number)=><div className="hq2User" key={i}><div><strong>{eventName(a.action)}</strong><div className="hq2Sub">{a.districtName} · {a.actor_email||'District Admin / System'} · {a.target_type||'record'}</div></div><span className="hq2Sub">{a.created_at?new Date(a.created_at).toLocaleString():''}</span></div>):<div className="hq2Empty">No matching activity.</div>}</section>}

 {view==='settings'&&<><section className="hq2Grid2"><div className="hq2Card"><h2>Owner & company</h2><div className="hq2Ops"><div className="hq2Op"><strong>Owner account</strong><span>{user.email}</span></div><div className="hq2Op"><strong>Settings last saved</strong><span>{settingsMeta.updated_at?new Date(settingsMeta.updated_at).toLocaleString():'Never'}</span></div></div><div className="hq2FormGrid" style={{marginTop:14}}><div className="hq2Field"><label>Support email</label><input value={settings.support_email||''} onChange={e=>setSettings((c:any)=>({...c,support_email:e.target.value}))}/></div><div className="hq2Field"><label>Billing email</label><input value={settings.billing_email||''} onChange={e=>setSettings((c:any)=>({...c,billing_email:e.target.value}))}/></div><div className="hq2Field full"><label>Support URL</label><input value={settings.support_url||''} onChange={e=>setSettings((c:any)=>({...c,support_url:e.target.value}))}/></div></div></div><div className="hq2Card"><h2>Integrations</h2><div className="hq2Ops"><div className="hq2Op"><strong>Supabase</strong><span>Connected · {districts.length} tenants</span></div><div className="hq2Op"><strong>Stripe</strong><span>{districts.some(d=>d.billing)?'Billing records present':'Not connected'}</span></div><div className="hq2Op"><strong>Chrome telemetry</strong><span>{installedCount} installation reports</span></div><div className="hq2Op"><strong>Email</strong><span>{settings.support_email?'Support email configured':'Not configured'}</span></div></div></div></section><section className="hq2Card"><div className="hq2CardHeader"><div><h2>Global customer defaults</h2><p>Applied as defaults when you create future districts.</p></div><button className="hq2Primary" onClick={saveSettings}>Save defaults</button></div><div className="hq2FormGrid"><div className="hq2Field"><label>Default trial days</label><input type="number" value={settings.default_trial_days} onChange={e=>setSettings((c:any)=>({...c,default_trial_days:+e.target.value}))}/></div><div className="hq2Field"><label>Default grace days</label><input type="number" value={settings.default_grace_days} onChange={e=>setSettings((c:any)=>({...c,default_grace_days:+e.target.value}))}/></div><div className="hq2Field"><label>Default seats</label><input type="number" value={settings.default_seats} onChange={e=>setSettings((c:any)=>({...c,default_seats:+e.target.value}))}/></div><div className="hq2Field"><label>Minimum Dock version</label><input value={settings.default_minimum_version||''} onChange={e=>setSettings((c:any)=>({...c,default_minimum_version:e.target.value}))}/></div><div className="hq2Field"><label>Default plan</label><select value={settings.default_plan} onChange={e=>setSettings((c:any)=>({...c,default_plan:e.target.value}))}><option>district</option><option>school</option><option>pilot</option></select></div><div className="hq2Field"><label>Default theme</label><select value={settings.default_theme} onChange={e=>setSettings((c:any)=>({...c,default_theme:e.target.value}))}>{BUILTIN.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></div></div></section><section className="hq2Grid2"><div className="hq2Card"><h2>Notifications</h2>{[['Payment failures','notify_payment_failed'],['Trials ending','notify_trial_ending'],['Renewals','notify_renewal'],['Suspensions','notify_suspension'],['Seat thresholds','notify_seat_threshold'],['Workspace failures','notify_workspace_failure'],['Incidents','notify_incident'],['Outdated versions','notify_outdated_version'],['New admins','notify_new_admin']].map(([l,k])=><label className="hq2User" key={k}><strong>{l}</strong><input type="checkbox" checked={!!settings[k]} onChange={e=>setSettings((c:any)=>({...c,[k]:e.target.checked}))}/></label>)}</div><div className="hq2Card"><h2>Theme Store readiness</h2><p>Theme creation, publishing and global/district assignment are now isolated in HQ. Commerce entitlements, purchases, refunds and delivery remain a separate next system so we can design the licensing rules correctly before taking money.</p></div></section><section className="hq2Card"><h2>Danger zone</h2><p style={{marginBottom:12}}>These flags are persisted owner settings. Product-wide enforcement will only be wired after a dedicated safety test.</p><div className="hq2Inline"><label className="hq2Check"><input type="checkbox" checked={!!settings.maintenance_mode} onChange={e=>setSettings((c:any)=>({...c,maintenance_mode:e.target.checked}))}/> Maintenance mode flag</label><label className="hq2Check"><input type="checkbox" checked={!!settings.emergency_restrictions} onChange={e=>setSettings((c:any)=>({...c,emergency_restrictions:e.target.checked}))}/> Emergency restrictions flag</label><button className="hq2Danger" onClick={saveSettings}>Save safety flags</button></div></section></>}

 <div className="hq2Notice">{status}</div></main></div></div>
}
