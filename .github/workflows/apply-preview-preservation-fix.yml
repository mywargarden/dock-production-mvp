name: Apply preview preservation convergence fix

on:
  push:
    branches:
      - release-dock-0-3-12-continuity
    paths:
      - scripts/apply_preview_preservation_fix.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: release-dock-0-3-12-continuity
          fetch-depth: 0
      - name: Apply assertion-guarded preview fix
        run: python3 scripts/apply_preview_preservation_fix.py
      - name: Verify extension static gate
        run: bash scripts/static_check.sh
      - name: Remove one-shot patch tooling
        run: git rm scripts/apply_preview_preservation_fix.py .github/workflows/apply-preview-preservation-fix.yml
      - name: Commit production fix
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dock-extension/core/storage.js
          git commit -m "Keep inline previews authoritative across reorder"
          git push origin HEAD:release-dock-0-3-12-continuity
