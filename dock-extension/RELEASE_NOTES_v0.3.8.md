# Dock 0.3.8 — Managed Background Fit + Drag Ghost Anchor

- Preserves the full administrator-selected managed Dock background instead of cropping tall or wide artwork.
- Uses the district soft background color to fill unused canvas around contained artwork.
- Aligns District Admin Staff Preview framing with the extension so preview better predicts the staff experience.
- Keeps the memory-card drag preview fixed to viewport coordinates so the dragged tile stays attached to the pointer instead of dropping far below the hand.
- Does not change memory ordering logic, Notes persistence, authentication, workspace authority, or managed-content behavior.

Candidate status: requires one real Chrome visual validation pass covering managed background framing and memory-card drag-preview anchoring before release promotion.
